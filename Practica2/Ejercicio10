#A una cerealera van T camiones a descargarse trigo y M camiones a descargar maíz. Sólo hay lugar para que 7 camiones a la vez descarguen, 
#pero no pueden ser más de 5 del mismo tipo de cereal.

#b) Implemente una solución que no use procesos adicionales (sólo camiones). No importa el orden de llegada para descargar. Nota: maximice la concurrencia.

sem lugaresLibres = 7; // para proteger las variables compartidas
sem camionTrigo = 5; // para que los camiones de trigo esperen
sem camionMaiz = 5; // para que los camiones de maíz esperen
int cantCamionesTrigo = T; // contador de camiones de trigo descargando
int cantCamionesMaiz = M; // contador de camiones de maíz descargando

b)
Process CamionTrigo[id: 1..T] {
    P(camionTrigo); // espera que no haya más de 5 camiones de trigo
    P(lugaresLibres); // espera que haya un lugar libre
    descargarTrigo();
    V(lugaresLibres); // libera el lugar
    V(camionTrigo); // libera el permiso para camiones de trigo
}

Process CamionMaiz[id: 1..M] {
    P(camionMaiz); // espera que no haya más de 5 camiones de maíz
    P(lugaresLibres); // espera que haya un lugar libre
    descargarMaiz();
    V(lugaresLibres); // libera el lugar
    V(camionMaiz); // libera el permiso para camiones de maíz
}


